<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
</head>
<body>
    <!-- Add your EJS code here -->
  <% include partials/_header %>


    <ul>
          <%console.log(flag)%>

    <% if (flag == 0) { %>
      <a href="/login"><h1> Please Login to continue</h1></a>
      <% } else {%>
        <% for(let key in flag) { %>
            <% if(flag[key].longURL) { %>
              <li>Link: <%= flag[key].longURL %>
                and Shortened Form:
                <%= key %>
                <form method="POST" action="/urls/<%=key%>">
                <input type="submit" value="DELETE"></form>
                <form method="GET" action="/urls/<%=key%>">
                  <button type="submit">EDIT</button>
                </form>
            <% } %>
            </li>
        <% } %>
      <% } %>
    </ul>

<% include partials/_footer %>
</body>
</html>